PCIe上行接口,支持1个外部PCIe上行接口（性能Gen3），支持13个输出到外部的PCIe下行接口（性能Gen3）。PCIe接口交换,支持输入PCIe接口分时控制输出到13个Slot的PCIe接口。LVDS接口,支持单板主控与13个Slot的LVDS通信（频率、带宽等性能）。时钟接口,支持输出到13个Slot的PCIe时钟接口（S21、S11、阻抗等性能）支持输出到13个Slot的PCIe时钟接口（S21、S11、阻抗等性能）支持输出到13个Slot的固定系统同步时钟接口（S21、S11、阻抗等性能）;Reset接口,支持1路光纤卡reset信号输入接口，输出到13个Slot的reset接口。trigger接口,支持输出到13个Slot各8个双向单端接口（S21、S11、阻抗等性能）支持输出到13个Slot各1个单向（背板到Slot）差分接口（S21、S11、阻抗等性能），对外2个双向单端SMA接口（S21、S11、阻抗等性能）!板卡访问接口,支持输出到13个Slot各1个IIC通讯接口（速率性能）。加载状态指示接口,支持输出到13个Slot各1个IO电平状态指示接口。Slot地址指示接口,支持输出到13个Slot各5个IO电平地址指示接口。Slot相邻连接接口,支持输出到13个Slot各2个IO相邻连接接口！电源输出接口,支持输出到13个Slot各1个12V电源接口支持输出到13个Slot各1路3.3V电源接口。电源输入接口,支持1路48V电源输入接口
整机控制信号接口,支持1路整机控制LVDS接口（温度监测、风扇调节、LED点灯、电源状态访问）支持1路光纤卡整机控制GPIO接口。我司相比爱得万IO通道数少。光纤卡控制信号接口,支持1路光纤卡控制.IIC通讯接口，支持2路光纤卡控制IIC地址选择接口。竞争对手泰瑞达DPS输出噪声低于我司1mV。本地时钟产生,支持10/12.5MHz时钟产生，7044（频率12.5MHz、Jitter<1ps、频偏<20ppm 等性能）。支持固定系统同步时钟产生，CCHD（频率100MHz、Jitter<0.5ps、频偏<20ppm 等性能）;支持可配系统同步时钟产生，7044（频率100M/200MHz、Jitter<1ps、频偏<20ppm 等性能）。支持trigger系统同步时钟产生（频率100MHz、Jitter<0.5ps、频偏<20ppm 等性能）；时钟参考选择,支持10/12.5MHz时钟产生源来自SMA外部输入。时钟接口支持10/12.5MHz时钟产生源来自固定系统同步时钟源，CCHD。支持固定系统同步时钟产生源来自板级晶振，CCHD。时钟传输,支持固定系统同步时钟传输到13个Slot的时钟接口，CCHD。支持可配系统同步时钟传输到13个Slot的时钟接口，7044。支持外部10/12.5MHz时钟传输到13个Slot的时钟接口，SMA。支持固定系统同步时钟传输到板级主控FPGA，CCHD。时钟检测,支持外部10/12.5MHz时钟频偏检测。PCIe时钟选择,支持根据硬件设置选择来自PCIe接口的时钟？支持根据硬件设置选择来自本地固定系统同步时钟。PCIe时钟分发控制,支持选择的时钟分发到13个Slot的时钟接口?trigger时钟选择,支持选择固定系统同步时钟源产生trigger时钟支持选择SMA外部输入时钟源产生trigger时钟。时钟相位同步,支持13个Slot同步信号检测。支持13个Slot同步相位配置与调整。支持背板Trigger脉冲宽度可配置，每个Slot各8路Trigger，根据寄存器配置值选择Slot之间的Triger组合。板卡温度检测,支持板级温度检测上报寄存器，精度0.125度。风扇检测与控制,支持检测板级风扇工作检测（K7、8796），检测板级风扇转速控制。